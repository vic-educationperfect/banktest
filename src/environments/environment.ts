export const environment = {
  production: false,
  baseUrl: window.location.origin, // Base URL here
  apiUrl: 'local', // global API URL (for production build)
  adminApiUrl: 'local/admin'
  //... more of your variables
};